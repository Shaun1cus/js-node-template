const fullName = 'Shaun Putnam';

const courseName = 'Web Development I';

const expectedGrade = 100;

const message = fullName + ' is taking ' + courseName + ' and expects to earn ' + expectedGrade + '%.';

const betterMessage = `${fullName} is taking ${courseName} and expects to earn ${expectedGrade}%.`;

console.log(message);
console.log(betterMessage);